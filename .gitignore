# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code: launch.json é compartilhável, settings.local.json é da máquina
.claude/settings.local.json
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel: vínculo local com projeto/conta (orgId + projectId). Nunca versionar,
# senão o repo fixa uma conta e quebra ao trocar de Vercel.
.vercel

# Fotos originais dos heros (fonte dos .webp). ~47MB, nenhuma referenciada no
# código — ficam na máquina, não no histórico.
src/assets/hero-2/
